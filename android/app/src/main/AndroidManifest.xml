<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Уведомления и точные напоминания -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Чтобы просить исключение из оптимизации батареи (EMUI/MIUI режут
         фоновые будильники — без этого уведомления не приходят). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Биометрия -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Сеть (Яндекс.Диск) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="Лея"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher_cream"
        android:allowBackup="false">

        <!-- ОБЯЗАТЕЛЬНО для flutter_dynamic_icon_plus: без этого сервиса
             смена иконки на Android НЕ применяется (команда проходит без
             ошибки, но алиас не переключается и сбрасывается при перезапуске). -->
        <service
            android:name="com.solusibejo.flutter_dynamic_icon_plus.FlutterDynamicIconPlusService"
            android:stopWithTask="false" />

        <!-- MainActivity: enabled="false" и БЕЗ launcher intent-filter.
             ПОЧЕМУ enabled=false: Flutter-тулинг (aapt) ищет точку входа и,
             если MainActivity включена, требует LAUNCHER именно на ней.
             Выключив её, мы заставляем тулинг найти включённый alias
             (.MainActivityCream) как точку входа.
             ПОЧЕМУ без LAUNCHER: если LAUNCHER стоит и здесь, и на алиасах,
             при включении алиаса MainActivity остаётся включённой →
             в лаунчере появляются ДВЕ иконки.
             Сама активность запускается через targetActivity алиасов. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher_cream"
            android:roundIcon="@mipmap/ic_launcher_cream"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ИКОНКА: Румянец (alias, выключен по умолчанию) -->
        <activity-alias
            android:name=".MainActivityBlush"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_blush"
            android:roundIcon="@mipmap/ic_launcher_blush"
            android:label="Лея"
            android:targetActivity=".MainActivity">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- ИКОНКА: Тёмная (alias, выключен по умолчанию) -->
        <activity-alias
            android:name=".MainActivityDark"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark"
            android:roundIcon="@mipmap/ic_launcher_dark"
            android:label="Лея"
            android:targetActivity=".MainActivity">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
